import 'package:flutter/material.dart';

kSizedBox({double hight = 0, double width = 0}) {
  return SizedBox(
    height: hight,
    width: width,
  );
}

const labelTextStyle = TextStyle(color: Colors.grey, fontSize: 16);

final textFormfieldColor = Colors.grey.shade200;
